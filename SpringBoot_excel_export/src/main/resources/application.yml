spring:
  datasource:
    username: root
    password: tiger
    url: jdbc:mysql://localhost:3306/curd
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
